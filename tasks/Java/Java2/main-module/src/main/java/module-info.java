module main.module {
    requires calculation;
    requires csv.reader;
    requires db.reader;
    requires json.output;
    requires normal.check;
    requires plot.output;
    requires commons.cli;
    requires common;
}